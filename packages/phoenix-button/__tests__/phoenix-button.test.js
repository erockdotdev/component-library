'use strict';

const phoenixButton = require('..');
const assert = require('assert').strict;

assert.strictEqual(phoenixButton(), 'Hello from phoenixButton');
console.info('phoenixButton tests passed');
